// Placeholder shell. Real screens are wired in subsequent checkpoints.
export default function App(): JSX.Element {
  return (
    <main className="app-shell">
      <h1>Battleship</h1>
      <p>Naval command — coming online.</p>
    </main>
  );
}
